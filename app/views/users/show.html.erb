
     <%= image_tag current_user.display_profile_picture if current_user.profile_picture.attached? %>


<h1>WELCOME <%= current_user.username %></h1>

<%= current_user.email %></br>

<%= button_to "Logout", logout_path %>
<%= button_to "Delete Account", user_path(current_user), method: :delete %> 
<%= link_to "Edit Account", edit_user_path(current_user)%> 

<h3><%= link_to "Your Favorites", user_favorites_path(current_user) %></h3>
</br>
<h3>Your reviews</h3>
<%= render(partial: 'reviews/review', collection: @reviews) || "No reviews found"%>

 
