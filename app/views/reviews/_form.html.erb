<%= render partial: 'layouts/error', locals: {object: @review} %>

<%= form_with(model: @review, url: strain_reviews_path) do |f| %>
    <%= f.hidden_field :strain_id %>
    <%= f.hidden_field :user_id %>
    
    <br>
    <%= f.label :context %>
    <%= f.text_area :context %>
    </br>

    <%= f.label :rating %>
    <%= f.number_field :rating %>
  
    <%= f.submit %>
  <% end %>