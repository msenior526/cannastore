<h1>Are you at least 21 years of age?</h1>
# Two buttons , yes and no
# If yes, user is redirected to strain index
# If no, flash error 
</br>
</br>
</br>
</br>

<%= form_with(url: root_path, method: :get) do |f| %>
  <%= f.check_box :yes %>
  <%= f.check_box :no %>
  <%= f.submit "Submit" %>
<% end %>