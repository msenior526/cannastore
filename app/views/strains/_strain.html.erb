<div class="col">
    <div class="card">
    <div class="card-body">
      <h5 class="card-title"> <%= strain.name %></h5>
      <p class="card-text"><%= strain.category.name %></p>
      <p class="card-text"><%= strain.description %></p>
      <a href="<%= strain_path(strain) %>" class="btn btn-primary">view strain page</a>
    </div>
  </div>
</div>
